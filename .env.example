# Client Configuration
# WebSocket URL - defaults to same-origin /ws endpoint when not set
# For development: ws://localhost:3503/ws
# For production behind nginx: leave empty to auto-detect
VITE_WS_URL=

# Port for the development server
VITE_PORT=3503

# Enable mock WebSocket mode for development without gateway
VITE_MOCK_WS=false

# Server Configuration
# Port for the production server (Express + WebSocket)
PORT=3503

# OpenClaw Gateway WebSocket URL for server-side proxy
GATEWAY_WS_URL=ws://127.0.0.1:18789

# Allowed CORS origins (comma-separated, for production)
# Leave empty for development (allows all origins)
# Example: ALLOWED_ORIGINS=https://agentviz.setrox.com.tr
ALLOWED_ORIGINS=
